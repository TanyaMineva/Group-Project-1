/*const http = require('http');  // We import the default http package
                               // it's stored in the http const
const app = require('./backend/app');  // Listener to incoming requests

const port = process.env.PORT || 3000;

app.set('port', port);
const server = http.createServer(app);


server.listen(port); // We activate the server with a port number from the host provider
                                         // environment variable, injected by runtime (if set)

*/
